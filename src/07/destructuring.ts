export const props = {
    name: 'Name',
    age: 30,
    lessons: [{title: 1}, {title: 2}]
}

// const name = props.name;
// const age = props.age;
// const lessons = props.lessons;
//одно и тоже
// const {name, age, lessons} = props;